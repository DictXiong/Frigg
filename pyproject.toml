[project]
name = "frigg"
version = "1.0.0"
description = "api.dxng.cn"
authors = [{ name="Dict Xiong", email="me@dxng.cn" }]
requires-python = ">=3.7"
dependencies = [
    "flask",
    "flask-limiter",
    "pyyaml",
    "pypushdeer",
    "cloudflare",
    "mysql-connector-python"
]

[project.scripts]
frigg = "frigg.frigg:main"
frigg-cli = "frigg.frigg_cli:main"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
